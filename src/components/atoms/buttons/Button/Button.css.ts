import { style } from "@vanilla-extract/css";

export const buttonStyle = style({
  padding: "8px 16px",
  border: "none",
  borderRadius: "4px",
  outline: "none",
  fontSize: "1.4rem",
  cursor: "pointer",
  backgroundColor: "#efefef",
});
